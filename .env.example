# Local SQLite is the default. For PostgreSQL, use the URL below.
DATABASE_URL=sqlite:///./ledger.db
SERIALIZABLE_TRANSACTIONS=true

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg://ledger:ledger@localhost:5432/ledger
